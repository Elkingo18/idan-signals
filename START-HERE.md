# ⚡ Idan Trader v3 — מה בניתי, ומה אתה צריך לעשות

**זמן עבודה שלך: ~25 דקות. עלות חודשית קבועה: $0** (חוץ ממנוי TradingView אחד — ראה שלב 3).
אחרי 25 הדקות האלה המערכת רצה **לבד**, בלי קלוד, בלי AI, בלי שרת, בלי שתשלם לאף אחד על פיתוח.

---

## 🎯 התשובה לשאלה ששאלת (מנוי TradingView)

> "אני לא יודע איזה מנוי לעשות בטריידינגויו כדי שזה יהיה אוטומטי"

**התשובה: TradingView Essential — $12.95 לחודש (בתשלום שנתי).** זה המנוי הזול ביותר שיש בו
Webhook והתראות שרץ בצד השרת ולא נגמרות. הוא נותן 20 התראות במקביל — כלומר 20 ניירות + זהב.

**ואל תשדרג מעבר לזה בשלב הזה.** Plus ($29.95) נותן 100 התראות, אבל על ארנק של $1000
זה 3% מההון בחודש — בזבוז. המנוע החינמי שבניתי לך סורק את כל ה-77 המניות בלי לשלם כלום,
ו-TradingView צריך להתריע רק על ה-20 החמות. כשההון יגיע ל-$5,000 — אז תשדרג ל-Plus.

**חשוב שתדע:** לאוטומציה עצמה **אתה בכלל לא צריך מנוי**. המנוע שבניתי סורק, מחליט, פותח וסוגר
לבד בלי TradingView. המנוי הוא בשביל מה **שאתה** ביקשת — שהאיתותים יגיעו כפוש לטלפון
מ-TradingView עצמו, ושתראה אותם על הגרף. זה שווה $12.95. יותר מזה — לא.

---

## 🧩 מה קיבלת

| קובץ | מה זה עושה |
|---|---|
| `index.html` | **האפליקציה.** משחק/פלטפורמה: דרגות, XP, רצפים, בוס חודשי, הישגים, ולוח-שנה שכל יום בו ירוק/אדום ולחיצה עליו פותחת כל מה שקרה. עובד בטלפון ובמחשב, אפשר להתקין כאפליקציה. |
| `engine/engine.py` | **המנוע.** סורק, מחשב, מחליט, פותח וסוגר פוזיציות בארנק הנייר של $1000, מזיז סטופים, מוציא חצי ביעד. ללא AI. |
| `engine/config.json` | כל המספרים במקום אחד — סיכון, תקרות, פרמטרים. משנים כאן, לא בקוד. |
| `engine/selftest.py` | 60+ בדיקות. רצות **לפני** כל מסחר. אם הלוגיקה נשברה — המערכת לא סוחרת. |
| `.github/workflows/trade.yml` | **התזמון.** מריץ את המנוע לבד כל 30 דקות בשעות המסחר, חינם, לנצח. |
| `pine/idan-signals-v3.pine` | האינדיקטור ל-TradingView, עם ההתראות. |

---

## שלב 1 — להעלות לגיטהאב (7 דקות, בלי טוקנים ובלי טרמינל)

1. היכנס ל־`https://github.com/Elkingo18/idan-signals`
2. **Add file → Upload files** → גרור לשם את התיקיות `engine`, `data`, `pine`, `.github`
   ואת `index.html`. (אם גיטהאב לא מקבל תיקייה בגרירה — פתח אותה וגרור את הקבצים שבתוכה.)
3. למטה: **Commit changes**.
4. **Settings → Pages** → Source: `Deploy from a branch`, Branch: `main`, Folder: `/ (root)` → Save.
5. **Settings → Actions → General** → גלול ל־"Workflow permissions" → סמן
   **Read and write permissions** → Save. ← *בלי זה המנוע לא יוכל לשמור את הארנק.*

> 💡 **הטוקן שחיפשנו בפעם הקודמת — לא צריך אותו יותר.** ריצה בתוך גיטהאב מקבלת הרשאה
> אוטומטית. זה מה שפתר את המשימה שהייתה תקועה.

## שלב 2 — להריץ פעם ראשונה (2 דקות)

**Actions** → `Idan Signals — auto paper trading` → **Run workflow** → Run.
תוך ~2 דקות תראה ✅ ירוק. מאותו רגע זה רץ לבד כל 30 דקות.

הכתובת שלך (שמור במסך הבית בטלפון וגם במחשב):
```
https://elkingo18.github.io/idan-signals/
```
בטלפון: פתח בכרום/ספארי → תפריט → **הוסף למסך הבית**. זה ייראה ויתנהג כאפליקציה.

## שלב 3 — TradingView (10 דקות)

1. קנה **Essential** ($12.95/חודש, תשלום שנתי) — `tradingview.com/pricing`.
2. Pine Editor → הדבק את `pine/idan-signals-v3.pine` → **Add to chart** → שמור.
3. צור רשימת מעקב עם 20 השמות האלה (אלה שהמנוע מנטר בפועל):
   `SOFI F RIVN LCID PLUG SNAP NIO AAL LYFT NU RUN BBAI SOUN AUR NVTS HBAN GT KVUE ERIC NOK`
4. על כל אחד: 🕐 **Alert** → Condition = `Idan Signals v3` → `Idan • כל איתות`
   → Options: **Once Per Bar Close** → Expiration: **Open-ended** → Notifications: ✅ **Push**.
5. באפליקציית TradingView בטלפון: Settings → Notifications → אשר פוש.
6. לזהב: פתח גרף `OANDA:XAUUSD` בטיים-פריים 5 דקות → בהגדרות האינדיקטור הדלק
   **"הפעל זהב"** → צור התראה נוספת.

> ⚠️ **אל תדליק** את המתג `שלח JSON לוובהוק לביצוע אוטומטי`. הוא כבוי בכוונה.
> הוא שולח פקודות ביצוע אוטומטיות, וזה מסוכן עד שהאסטרטגיה תאומת. המנוע כבר עושה
> את הביצוע בנייר בשבילך — אין צורך בו.

## שלב 4 — לכבות את שני הבוטים במטה-טריידר (3 דקות, אתה צריך לעשות את זה)

**את זה אני לא יכול לעשות בשבילך מרחוק** — אין לי גישה לתוכנות שמותקנות אצלך על Windows,
רק לקבצים בתיקיות שתחבר. אלה בדיוק הצעדים:

1. **להסיר את הרובוטים מהגרפים:** פתח MetaTrader → על כל גרף שיש בו סמיילי בפינה
   הימנית העליונה: לחיצה ימנית על הגרף → **Expert Advisors → Remove**.
   (או: Navigator → Expert Advisors → לחיצה ימנית על הבוט → Remove from chart.)
2. **לכבות מסחר אוטומטי גלובלי:** בסרגל העליון — כפתור **AutoTrading / Algo Trading**
   שיהיה **אדום**. גם: Tools → Options → Expert Advisors → בטל את
   `Allow automated trading`.
3. **שהמטה-טריידר לא יעלה כשהמחשב נדלק** (זה מה שתוקע לך את המחשב):
   `Ctrl+Shift+Esc` → לשונית **Startup apps** → מצא `MetaTrader 5` / `MetaTrader 4` /
   `Terminal` → לחיצה ימנית → **Disable**.
4. אם הוא עדיין עולה: `Win+R` → הקלד `shell:startup` → מחק משם קיצור של MetaTrader.
   ואם גם זה לא — `Win+R` → `taskschd.msc` → חפש MetaTrader → Disable.
5. **סגור את MetaTrader עם AutoTrading אדום.** MT שומר את מצב הגרפים בסגירה, ולכן
   אם תסגור אותו כשהבוט עוד מחובר — הוא יחזור בפעם הבאה.

---

## 🛑 קרא את זה לפני ה-10.8 — זה החלק הכי חשוב במסמך

ביקשת שאבקר את האסטרטגיה. ביקרתי אותה לעומק, וזאת התוצאה. לא אנעים לך:

**האסטרטגיה לא מוכנה לכסף אמיתי ב-10.8.**

* **סווינג:** 146 עסקאות, יתרון של **+0.07R** לעסקה. במדידה סטטיסטית זה p≈0.20 —
  כלומר **לא ניתן להבחין בין זה לאפס**. כדי להוכיח יתרון בגודל כזה צריך ~1,000 עסקאות.
  גרוע מזה: הפרמטרים (`volFactor`, `pivLen`) נבחרו על אותו מדגם שבו נבדקו — זה לא בדיקה,
  זה התאמה בדיעבד. ובעיקר: הבקטסט **לא כלל עמלות וסליפג'**, והכניסה חושבה בסגירת נר
  האיתות (בפועל תיכנס בפתיחה הבאה). כשמוסיפים את זה, +0.07R הופך ל־**כ־(-0.03R)** —
  כלומר מערכת מפסידה.
* **זהב:** 19 עסקאות ב-10.5 ימים, רג'ים אחד, נבדק על חוזה עתידי (GC) אבל נסחר כ-CFD.
  זה לא מדגם, זה רמז.
* **דיי-טרייד:** היה PF 0.93 — **מפסיד**. שלושת הפילטרים שנוספו נבחרו *אחרי* שראינו את
  הכישלון ולא נבדקו מחדש. לכן הוא מסומן 🧪 **נייר בלבד** ולא ייגע בכסף אמיתי.
* **4% לעסקה זה Kelly מלא** על יתרון לא מוכח. במקום כזה דרודאון של 50% הוא **המסלול
  הצפוי**, לא תרחיש קיצון. הבקטסט עצמו הראה דרודאון של 12.8R = **51% מהחשבון**.
* **מגבלה מבנית:** IBKR דורש $2,000 מינימום למרג'ין. חשבון של $1000 = מזומן בלבד →
  **כל איתות שורט לא בר-ביצוע**, ו-PDT חוסם דיי-טרייד ממילא.
* **זהב ב-4%:** דורש ~10 אונקיות = חשיפה של $40,500 על $1000. במינוף 1:20 המותר
  בברוקר מפוקח זה דורש $2,025 מרג'ין — **יותר מכל הארנק**. מתמטית בלתי אפשרי.
  לכן הורדתי את הזהב למינוף 1:20 ותקרת מרג'ין של 35% מההארנק.

### מה עשיתי כדי שזה יהיה בטוח בכל זאת

השארתי לך 4% כמו שבחרת, אבל הוספתי את שכבת ההגנה שלא הייתה קיימת:

| הגנה | הערך |
|---|---|
| מקסימום פוזיציות פתוחות | 3 |
| חשיפת סיכון כוללת (heat) | 12.5% מההון |
| מקסימום פוזיציה אחת **לכל סקטור** | ← זה מה שמונע ש-3 מניות EV ייפלו יחד |
| מקסימום גודל פוזיציה אחת | 35% מההון |
| עצירת הפסד יומית / שבועית | 6% / 12% — ואז המערכת מפסיקה לפתוח |
| מינימום יחס סיכון/סיכוי | 1.5 — פחות מזה נחסם |
| שורט | חסום |
| תקופת קירור אחרי הפסד | 3 סריקות באותו נייר |
| עמלות + סליפג' + ספרד | נגבים על כל עסקה. המספרים שתראה הם **נטו**. |

**התוצאה:** בגלל תקרת הגודל, הסיכון האמיתי לעסקה יוצא בפועל **1.5%–3%** ולא 4%.
זה בכוונה, וזה מה שמציל חשבון של $1000.

### ההמלצה שלי ל-10.8

**אל תפקיד כסף ב-10.8. תן לזה לרוץ בנייר 30–60 יום.**
תבוא לעידו טמין עם המסך הזה ועם הנתונים האמיתיים, ותשאל אותו את השאלה הנכונה:
*"האם 60 יום של מסחר נייר עם +0.07R לעסקה מצדיקים הפקדה?"*
אם התוצאות בנייר יתאימו למה שהבקטסט הבטיח — תפקיד בביטחון. אם לא — חסכת $1000.
זו לא זהירות מוגזמת; זו בדיוק העבודה שביקשת שאעשה.

**ומה שאני לא יכול להבטיח, ולא אבטיח:** שהמערכת "תרוויח הרבה כסף עד סוף החודש".
אף אחד לא יכול. מה שכן אפשר להבטיח: שהיא לא תאבד את הארנק בשבוע אחד, שהיא תעבוד
לבד בלי שתשלם על AI, ושכל מספר שתראה יהיה אמיתי.

---

## 🎮 מה יש באפליקציה

* **מצב** — דרגה, XP, בוס חודשי (יעד 8%), עקומת הון, רווח/הפסד ליום/שבוע/חודש/רבעון/
  חצי שנה/שנה, סולם צמיחה ל-$5,000, משימות יומיות, ופאנל "שומר הסיכון" שמראה בזמן אמת
  מה מגן עליך.
* **לוח שנה** — בדיוק מה שביקשת. כל יום ירוק/אדום עם הסכום. לחיצה על יום → כל העסקאות
  של אותו יום, כניסה, יציאה, סיבה, כמה R, ואחוז משמעת. 🔥 מסמן רצף.
* **איתותים** — עם ציון איכות 0–100 (נבנה מחדש; הציון הקודם נתן 96 לכל דבר ולכן היה חסר
  תועלת), ופאנל "מה נחסם ולמה".
* **פוזיציות** — פס התקדמות חי מהסטופ עד היעד.
* **הישגים** — 15 תגים לפתיחה. **נתונים** — הסטטיסטיקה האמיתית, אחרי עלויות.
* **אמת** — כל מה שכתוב פה, גם בתוך האפליקציה, כדי שלא תשכח.

---

## ❓ אם משהו לא עובד

| תסמין | פתרון |
|---|---|
| האתר מראה "מצב הדגמה" | המנוע עוד לא רץ. Actions → Run workflow. |
| ה-Action נכשל באדום | פתח אותו, קרא את השורה האדומה. אם זה `Permission denied` → שלב 1.5. |
| הארנק לא מתעדכן | Actions → בדוק שהריצה האחרונה ירוקה ושהיא עשתה commit ל-`data/state.json`. |
| אין התראות בטלפון | TradingView → Alerts → בדוק ✅ Push, ובאפליקציה שהפוש מאושר. |
| רוצה לשנות סיכון | `engine/config.json` → `risk_pct`. **אם תעלה מעל 4% — תעדכן גם `max_total_open_heat_pct`.** |
